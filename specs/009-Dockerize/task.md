# Tasks 009: Dockerización

- [x] T1 — Crear `Dockerfile` (multi-stage: build con Bun, producción
      con Nginx).
- [x] T2 — Crear `nginx.conf` con fallback SPA para React Router.
- [x] T3 — Crear `.dockerignore`.
- [x] T4 — Crear `docker-compose.yml`.
- [x] T5 — Documentar el flujo Docker en `README.md`.
- [x] T6 — Probar: `docker build` exitoso, `docker compose up` levanta
      el contenedor, la app carga en el navegador, y navegar directo a
      `/dashboard` (recargando la página, no solo navegando con clicks)
      no da 404.

## Orden sugerido
T1 → T2 → T3 → T4 → T5 → T6